import { ServiceByOrgnamePipe } from './service-by-orgname.pipe';

describe('ServiceByOrgnamePipe', () => {
  it('should be defined', () => {
    expect(new ServiceByOrgnamePipe()).toBeDefined();
  });
});
